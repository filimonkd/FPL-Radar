# Copy to .env and adjust. Never commit .env.
NODE_ENV=development
PORT=4000
# Local single-node replica set from docker-compose.yml (no auth, loopback only).
MONGODB_URI=mongodb://localhost:27017/fpl_radar?replicaSet=rs0
